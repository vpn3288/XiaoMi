# AI 审查流程

每一轮修改后：

```text
1. 主笔 AI 输出变更说明、风险点、回滚方式、测试命令、待审查文件
2. 为 Claude Code 打开新窗口审查
3. 为 OpenCode + DeepSeek Flash Free 打开新窗口审查
4. 为 Codex + GPT-5.5 打开新窗口审查
5. 三个审查者都只看最新脚本和最新指导文件
6. 主笔 AI 汇总意见并修改
```

禁止在旧窗口继续审查新脚本，避免旧上下文污染。

